export interface Colors {
  primary: {
    primary: string;
    dark: string;
    medium: string;
    light: string;
  };
  white: string;
  gray: {
    medium: string;
    light: string;
    dark: string;
  };
}
